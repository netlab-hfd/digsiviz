# DigSiViz backend — direct runtime dependencies.
#
# This file was rebuilt from a `pip freeze` that had captured a whole polluted
# system/global environment (Ubuntu apt packages, docs tooling, unrelated
# stacks). It now lists ONLY the packages the project source actually imports;
# pip resolves their transitive dependencies automatically. Everything that was
# removed is preserved below as commented lines, grouped by the reason it went.
#
# Verified to install and import in a clean venv (see README "Dependencies").

confluent-kafka==2.4.0
Flask==3.1.0
Flask-Cors==5.0.0
Flask-SocketIO==5.4.1
flatdict==4.1.0            # bumped from 4.0.1: 4.0.1 ships no wheel and its sdist
                          #   build imports pkg_resources, removed in setuptools>=81,
                          #   so it fails to install on modern toolchains. 4.1.0 has
                          #   a wheel; FlatDict(delimiter=...) API is unchanged.
flatten-json==0.1.14
pygnmi==0.8.13
PyYAML==6.0.1

# ----------------------------------------------------------------------------
# REMOVED — not importable from PyPI (Ubuntu/apt-managed, leaked into the freeze
# from the system Python). These are not project dependencies and break `pip
# install` on any other machine:
# cloud-init==24.2
# command-not-found==0.3
# dbus-python==1.2.18
# distro-info==1.1+ubuntu0.2
# launchpadlib==1.10.16
# lazr.restfulclient==0.14.4
# lazr.uri==1.0.6
# netifaces==0.10.6
# PyGObject==3.42.1
# python-apt==2.4.0+ubuntu4
# python-debian==0.1.43+ubuntu1.1
# systemd-python==234
# wadllib==1.3.6
# xkit==0.0.0
# gnmi==0.0.0a0            # pre-release stub, not on PyPI; project uses pygnmi
# sos==4.5.6
# ssh-import-id==5.11
#
# REMOVED — declared but never imported anywhere in the project source:
# deepdiff==6.7.1
# dictdiffer==0.9.0
#
# REMOVED — documentation build toolchain (Sphinx), not runtime:
# alabaster==0.7.16
# Babel==2.14.0
# docutils==0.20.1
# imagesize==1.4.1
# Pygments==2.17.2
# snowballstemmer==2.2.0
# Sphinx==7.3.7
# sphinx-rtd-theme==2.0.0
# sphinx-tabs==3.4.5
# sphinxcontrib-applehelp==1.0.8
# sphinxcontrib-devhelp==1.0.6
# sphinxcontrib-htmlhelp==2.0.5
# sphinxcontrib-jquery==4.1
# sphinxcontrib-jsmath==1.0.1
# sphinxcontrib-qthelp==1.0.7
# sphinxcontrib-serializinghtml==1.1.10
#
# REMOVED — unrelated stacks pulled in by the polluted freeze (Twisted, AMQP/
# Celery, keyring/secret storage, bogus 'socketio' shim):
# amqp==5.2.0
# Automat==20.2.0
# constantly==15.1.0
# hyperlink==21.0.0
# incremental==21.3.0
# jeepney==0.7.1
# keyring==23.5.0
# kombu==5.3.7
# PyHamcrest==2.0.2
# pyOpenSSL==21.0.0
# SecretStorage==3.3.1
# service-identity==18.1.0
# socketio==0.2.1         # NOT python-socketio; abandoned shim, would shadow it
# Twisted==22.1.0
# vine==5.1.0
# zope.interface==5.4.0
#
# REMOVED — transitive dependencies of the packages above; pip resolves these
# automatically from the direct deps, so they no longer need pinning here:
# annotated-types==0.7.0   attrs==21.2.0            bcrypt==3.2.0
# bidict==0.23.1           blinker==1.9.0           certifi==2024.2.2
# cffi==1.16.0             chardet==4.0.0           charset-normalizer==3.3.2
# click==8.1.7             colorama==0.4.4          configobj==5.0.6
# cryptography==42.0.4     distro==1.7.0            grpcio==1.60.0
# h11==0.14.0              httplib2==0.20.2         idna==3.7
# importlib-metadata==4.6.4 itsdangerous==2.2.0     Jinja2==3.1.3
# jsonpatch==1.32          jsonpointer==2.0         jsonschema==3.2.0
# MarkupSafe==2.1.5        more-itertools==8.10.0   oauthlib==3.2.0
# ordered-set==4.1.0       packaging==24.0          pexpect==4.8.0
# protobuf==4.25.1         ptyprocess==0.7.0        pyasn1==0.4.8
# pyasn1-modules==0.2.1    pycparser==2.21          pydantic==2.7.2
# pydantic_core==2.18.3    pyparsing==2.4.7         pyrsistent==0.18.1
# pyserial==3.5            python-engineio==4.10.1  python-magic==0.4.24
# python-socketio==5.11.4  pytz==2022.1             requests==2.31.0
# simple-websocket==1.1.0  six==1.16.0              tomli==2.1.0
# typing_extensions==4.12.0 urllib3==2.2.1          Werkzeug==3.1.3
# wsproto==1.2.0           zipp==1.0.0              PyJWT==2.3.0
# bidict / blinker etc. shown grouped only to keep this file short.
