# GENERATED by generate_manifest.py — DO NOT hand-edit. Change the tier table
# in the generator and re-run instead.
#
# Cascading downsample chain. Each tier = one Bucket (storage + retention) plus
# one Task (scheduled Flux downsample). Tier N reads tier N-1, applies
# mean/min/max/median over its window, writes suffixed fields (_mean/_min/_max/
# _median) into its bucket.
#
# Retention rule: a tier's retention >= the `every` of the task that reads FROM
# it, or the source expires before consumption. Each below = 2x that interval
# (floored to InfluxDB's 3600s minimum). Init bucket "infldb"
# (DOCKER_INFLUXDB_INIT_BUCKET) is the raw source — not defined here.

---
apiVersion: influxdata.com/v2alpha1
kind: Bucket
metadata:
  name: traffic-1m
spec:
  name: traffic-1m
  description: >-
    Traffic RATES in octets/s at 1m resolution, stored as
    mean/min/max/median fields. NOT counters: the rate is derived on a
    2s lattice before the first aggregation, so consumers must
    multiply by 8 for bits/s and must NOT apply derivative().
  retentionRules:
    - type: expire
      everySeconds: 3600   # floored to InfluxDB 1h minimum (2x rule -> 600s, rejected)

---
apiVersion: influxdata.com/v2alpha1
kind: Task
metadata:
  name: downsample-raw-to-1m
spec:
  name: downsample-raw-to-1m
  description: >-
    Derives the rate from raw infldb counters on a 2s lattice,
    then stores 1m mean/min/max/median OF THE RATE (octets/s).
  every: 1m
  offset: 30s
  query: |

    rate = from(bucket: "infldb")
      |> range(start: -62s)
      |> filter(fn: (r) => r._measurement == "network_interface")
      |> filter(fn: (r) => r._field == "statistics_out-octets" or r._field == "statistics_in-octets")
      |> aggregateWindow(every: 2s, fn: last, createEmpty: false, timeSrc: "_start")
      |> derivative(unit: 1s, nonNegative: true)
      |> range(start: -task.every)

    rate
      |> aggregateWindow(every: 1m, fn: mean, createEmpty: false)
      |> map(fn: (r) => ({r with _field: r._field + "_mean"}))
      |> to(bucket: "traffic-1m")

    rate
      |> aggregateWindow(every: 1m, fn: min, createEmpty: false)
      |> map(fn: (r) => ({r with _field: r._field + "_min"}))
      |> to(bucket: "traffic-1m")

    rate
      |> aggregateWindow(every: 1m, fn: max, createEmpty: false)
      |> map(fn: (r) => ({r with _field: r._field + "_max"}))
      |> to(bucket: "traffic-1m")

    rate
      |> aggregateWindow(every: 1m, fn: median, createEmpty: false)
      |> map(fn: (r) => ({r with _field: r._field + "_median"}))
      |> to(bucket: "traffic-1m")

---
apiVersion: influxdata.com/v2alpha1
kind: Bucket
metadata:
  name: traffic-5m
spec:
  name: traffic-5m
  description: >-
    Traffic RATES in octets/s at 5m resolution, stored as
    mean/min/max/median fields. NOT counters: the rate is derived on a
    2s lattice before the first aggregation, so consumers must
    multiply by 8 for bits/s and must NOT apply derivative().
  retentionRules:
    - type: expire
      everySeconds: 7200   # 2h = 2x the 1h consuming task

---
apiVersion: influxdata.com/v2alpha1
kind: Task
metadata:
  name: downsample-1m-to-5m
spec:
  name: downsample-1m-to-5m
  description: Downsamples the 1m tier to 5m (mean/min/max/median cascade).
  every: 5m
  offset: 30s
  query: |

    src = from(bucket: "traffic-1m")
      |> range(start: -task.every)
      |> filter(fn: (r) => r._measurement == "network_interface")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_mean" or r._field == "statistics_in-octets_mean")
      |> aggregateWindow(every: 5m, fn: mean, createEmpty: false)
      |> to(bucket: "traffic-5m")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_min" or r._field == "statistics_in-octets_min")
      |> aggregateWindow(every: 5m, fn: min, createEmpty: false)
      |> to(bucket: "traffic-5m")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_max" or r._field == "statistics_in-octets_max")
      |> aggregateWindow(every: 5m, fn: max, createEmpty: false)
      |> to(bucket: "traffic-5m")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_median" or r._field == "statistics_in-octets_median")
      |> aggregateWindow(every: 5m, fn: median, createEmpty: false)
      |> to(bucket: "traffic-5m")

---
apiVersion: influxdata.com/v2alpha1
kind: Bucket
metadata:
  name: traffic-1h
spec:
  name: traffic-1h
  description: >-
    Traffic RATES in octets/s at 1h resolution, stored as
    mean/min/max/median fields. NOT counters: the rate is derived on a
    2s lattice before the first aggregation, so consumers must
    multiply by 8 for bits/s and must NOT apply derivative().
  retentionRules:
    - type: expire
      everySeconds: 57600   # 16h = 2x the 8h consuming task

---
apiVersion: influxdata.com/v2alpha1
kind: Task
metadata:
  name: downsample-5m-to-1h
spec:
  name: downsample-5m-to-1h
  description: Downsamples the 5m tier to 1h (mean/min/max/median cascade).
  every: 1h
  offset: 30s
  query: |

    src = from(bucket: "traffic-5m")
      |> range(start: -task.every)
      |> filter(fn: (r) => r._measurement == "network_interface")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_mean" or r._field == "statistics_in-octets_mean")
      |> aggregateWindow(every: 1h, fn: mean, createEmpty: false)
      |> to(bucket: "traffic-1h")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_min" or r._field == "statistics_in-octets_min")
      |> aggregateWindow(every: 1h, fn: min, createEmpty: false)
      |> to(bucket: "traffic-1h")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_max" or r._field == "statistics_in-octets_max")
      |> aggregateWindow(every: 1h, fn: max, createEmpty: false)
      |> to(bucket: "traffic-1h")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_median" or r._field == "statistics_in-octets_median")
      |> aggregateWindow(every: 1h, fn: median, createEmpty: false)
      |> to(bucket: "traffic-1h")

---
apiVersion: influxdata.com/v2alpha1
kind: Bucket
metadata:
  name: traffic-8h
spec:
  name: traffic-8h
  description: >-
    Traffic RATES in octets/s at 8h resolution, stored as
    mean/min/max/median fields. NOT counters: the rate is derived on a
    2s lattice before the first aggregation, so consumers must
    multiply by 8 for bits/s and must NOT apply derivative().
  retentionRules:
    - type: expire
      everySeconds: 172800   # 2d = 2x the 1d consuming task

---
apiVersion: influxdata.com/v2alpha1
kind: Task
metadata:
  name: downsample-1h-to-8h
spec:
  name: downsample-1h-to-8h
  description: Downsamples the 1h tier to 8h (mean/min/max/median cascade).
  every: 8h
  offset: 30s
  query: |

    src = from(bucket: "traffic-1h")
      |> range(start: -task.every)
      |> filter(fn: (r) => r._measurement == "network_interface")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_mean" or r._field == "statistics_in-octets_mean")
      |> aggregateWindow(every: 8h, fn: mean, createEmpty: false)
      |> to(bucket: "traffic-8h")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_min" or r._field == "statistics_in-octets_min")
      |> aggregateWindow(every: 8h, fn: min, createEmpty: false)
      |> to(bucket: "traffic-8h")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_max" or r._field == "statistics_in-octets_max")
      |> aggregateWindow(every: 8h, fn: max, createEmpty: false)
      |> to(bucket: "traffic-8h")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_median" or r._field == "statistics_in-octets_median")
      |> aggregateWindow(every: 8h, fn: median, createEmpty: false)
      |> to(bucket: "traffic-8h")

---
apiVersion: influxdata.com/v2alpha1
kind: Bucket
metadata:
  name: traffic-1d
spec:
  name: traffic-1d
  description: >-
    Traffic RATES in octets/s at 1d resolution, stored as
    mean/min/max/median fields. NOT counters: the rate is derived on a
    2s lattice before the first aggregation, so consumers must
    multiply by 8 for bits/s and must NOT apply derivative().
  retentionRules:
    - type: expire
      everySeconds: 1209600   # 2w = 2x the 1w consuming task

---
apiVersion: influxdata.com/v2alpha1
kind: Task
metadata:
  name: downsample-8h-to-1d
spec:
  name: downsample-8h-to-1d
  description: Downsamples the 8h tier to 1d (mean/min/max/median cascade).
  every: 1d
  offset: 30s
  query: |

    src = from(bucket: "traffic-8h")
      |> range(start: -task.every)
      |> filter(fn: (r) => r._measurement == "network_interface")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_mean" or r._field == "statistics_in-octets_mean")
      |> aggregateWindow(every: 1d, fn: mean, createEmpty: false)
      |> to(bucket: "traffic-1d")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_min" or r._field == "statistics_in-octets_min")
      |> aggregateWindow(every: 1d, fn: min, createEmpty: false)
      |> to(bucket: "traffic-1d")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_max" or r._field == "statistics_in-octets_max")
      |> aggregateWindow(every: 1d, fn: max, createEmpty: false)
      |> to(bucket: "traffic-1d")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_median" or r._field == "statistics_in-octets_median")
      |> aggregateWindow(every: 1d, fn: median, createEmpty: false)
      |> to(bucket: "traffic-1d")

---
apiVersion: influxdata.com/v2alpha1
kind: Bucket
metadata:
  name: traffic-1w
spec:
  name: traffic-1w
  description: >-
    Traffic RATES in octets/s at 1w resolution, stored as
    mean/min/max/median fields. NOT counters: the rate is derived on a
    2s lattice before the first aggregation, so consumers must
    multiply by 8 for bits/s and must NOT apply derivative().
  retentionRules:
    - type: expire
      everySeconds: 4838400   # 8w = 2x the 4w consuming task

---
apiVersion: influxdata.com/v2alpha1
kind: Task
metadata:
  name: downsample-1d-to-1w
spec:
  name: downsample-1d-to-1w
  description: Downsamples the 1d tier to 1w (mean/min/max/median cascade).
  every: 1w
  offset: 30s
  query: |

    src = from(bucket: "traffic-1d")
      |> range(start: -task.every)
      |> filter(fn: (r) => r._measurement == "network_interface")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_mean" or r._field == "statistics_in-octets_mean")
      |> aggregateWindow(every: 1w, fn: mean, createEmpty: false)
      |> to(bucket: "traffic-1w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_min" or r._field == "statistics_in-octets_min")
      |> aggregateWindow(every: 1w, fn: min, createEmpty: false)
      |> to(bucket: "traffic-1w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_max" or r._field == "statistics_in-octets_max")
      |> aggregateWindow(every: 1w, fn: max, createEmpty: false)
      |> to(bucket: "traffic-1w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_median" or r._field == "statistics_in-octets_median")
      |> aggregateWindow(every: 1w, fn: median, createEmpty: false)
      |> to(bucket: "traffic-1w")

---
apiVersion: influxdata.com/v2alpha1
kind: Bucket
metadata:
  name: traffic-4w
spec:
  name: traffic-4w
  description: >-
    Traffic RATES in octets/s at 4w resolution, stored as
    mean/min/max/median fields. NOT counters: the rate is derived on a
    2s lattice before the first aggregation, so consumers must
    multiply by 8 for bits/s and must NOT apply derivative().
  retentionRules:
    - type: expire
      everySeconds: 14515200   # 24w = 2x the 12w consuming task

---
apiVersion: influxdata.com/v2alpha1
kind: Task
metadata:
  name: downsample-1w-to-4w
spec:
  name: downsample-1w-to-4w
  description: Downsamples the 1w tier to 4w (mean/min/max/median cascade).
  every: 4w
  offset: 30s
  query: |

    src = from(bucket: "traffic-1w")
      |> range(start: -task.every)
      |> filter(fn: (r) => r._measurement == "network_interface")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_mean" or r._field == "statistics_in-octets_mean")
      |> aggregateWindow(every: 4w, fn: mean, createEmpty: false)
      |> to(bucket: "traffic-4w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_min" or r._field == "statistics_in-octets_min")
      |> aggregateWindow(every: 4w, fn: min, createEmpty: false)
      |> to(bucket: "traffic-4w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_max" or r._field == "statistics_in-octets_max")
      |> aggregateWindow(every: 4w, fn: max, createEmpty: false)
      |> to(bucket: "traffic-4w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_median" or r._field == "statistics_in-octets_median")
      |> aggregateWindow(every: 4w, fn: median, createEmpty: false)
      |> to(bucket: "traffic-4w")

---
apiVersion: influxdata.com/v2alpha1
kind: Bucket
metadata:
  name: traffic-12w
spec:
  name: traffic-12w
  description: >-
    Traffic RATES in octets/s at 12w resolution, stored as
    mean/min/max/median fields. NOT counters: the rate is derived on a
    2s lattice before the first aggregation, so consumers must
    multiply by 8 for bits/s and must NOT apply derivative().
  retentionRules:
    - type: expire
      everySeconds: 29030400   # 48w = 2x the 24w consuming task

---
apiVersion: influxdata.com/v2alpha1
kind: Task
metadata:
  name: downsample-4w-to-12w
spec:
  name: downsample-4w-to-12w
  description: Downsamples the 4w tier to 12w (mean/min/max/median cascade).
  every: 12w
  offset: 30s
  query: |

    src = from(bucket: "traffic-4w")
      |> range(start: -task.every)
      |> filter(fn: (r) => r._measurement == "network_interface")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_mean" or r._field == "statistics_in-octets_mean")
      |> aggregateWindow(every: 12w, fn: mean, createEmpty: false)
      |> to(bucket: "traffic-12w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_min" or r._field == "statistics_in-octets_min")
      |> aggregateWindow(every: 12w, fn: min, createEmpty: false)
      |> to(bucket: "traffic-12w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_max" or r._field == "statistics_in-octets_max")
      |> aggregateWindow(every: 12w, fn: max, createEmpty: false)
      |> to(bucket: "traffic-12w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_median" or r._field == "statistics_in-octets_median")
      |> aggregateWindow(every: 12w, fn: median, createEmpty: false)
      |> to(bucket: "traffic-12w")

---
apiVersion: influxdata.com/v2alpha1
kind: Bucket
metadata:
  name: traffic-24w
spec:
  name: traffic-24w
  description: >-
    Traffic RATES in octets/s at 24w resolution, stored as
    mean/min/max/median fields. NOT counters: the rate is derived on a
    2s lattice before the first aggregation, so consumers must
    multiply by 8 for bits/s and must NOT apply derivative().
  retentionRules:
    - type: expire
      everySeconds: 62899200   # ~2y = 2x the 52w consuming task

---
apiVersion: influxdata.com/v2alpha1
kind: Task
metadata:
  name: downsample-12w-to-24w
spec:
  name: downsample-12w-to-24w
  description: Downsamples the 12w tier to 24w (mean/min/max/median cascade).
  every: 24w
  offset: 30s
  query: |

    src = from(bucket: "traffic-12w")
      |> range(start: -task.every)
      |> filter(fn: (r) => r._measurement == "network_interface")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_mean" or r._field == "statistics_in-octets_mean")
      |> aggregateWindow(every: 24w, fn: mean, createEmpty: false)
      |> to(bucket: "traffic-24w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_min" or r._field == "statistics_in-octets_min")
      |> aggregateWindow(every: 24w, fn: min, createEmpty: false)
      |> to(bucket: "traffic-24w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_max" or r._field == "statistics_in-octets_max")
      |> aggregateWindow(every: 24w, fn: max, createEmpty: false)
      |> to(bucket: "traffic-24w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_median" or r._field == "statistics_in-octets_median")
      |> aggregateWindow(every: 24w, fn: median, createEmpty: false)
      |> to(bucket: "traffic-24w")

---
apiVersion: influxdata.com/v2alpha1
kind: Bucket
metadata:
  name: traffic-52w
spec:
  name: traffic-52w
  description: >-
    Traffic RATES in octets/s at 52w resolution, stored as
    mean/min/max/median fields. NOT counters: the rate is derived on a
    2s lattice before the first aggregation, so consumers must
    multiply by 8 for bits/s and must NOT apply derivative().
  retentionRules:
    - type: expire
      everySeconds: 314496000   # ~10y = 2x the 260w consuming task

---
apiVersion: influxdata.com/v2alpha1
kind: Task
metadata:
  name: downsample-24w-to-52w
spec:
  name: downsample-24w-to-52w
  description: Downsamples the 24w tier to 52w (mean/min/max/median cascade).
  every: 52w
  offset: 30s
  query: |

    src = from(bucket: "traffic-24w")
      |> range(start: -task.every)
      |> filter(fn: (r) => r._measurement == "network_interface")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_mean" or r._field == "statistics_in-octets_mean")
      |> aggregateWindow(every: 52w, fn: mean, createEmpty: false)
      |> to(bucket: "traffic-52w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_min" or r._field == "statistics_in-octets_min")
      |> aggregateWindow(every: 52w, fn: min, createEmpty: false)
      |> to(bucket: "traffic-52w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_max" or r._field == "statistics_in-octets_max")
      |> aggregateWindow(every: 52w, fn: max, createEmpty: false)
      |> to(bucket: "traffic-52w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_median" or r._field == "statistics_in-octets_median")
      |> aggregateWindow(every: 52w, fn: median, createEmpty: false)
      |> to(bucket: "traffic-52w")

---
apiVersion: influxdata.com/v2alpha1
kind: Bucket
metadata:
  name: traffic-260w
spec:
  name: traffic-260w
  description: >-
    Traffic RATES in octets/s at 260w resolution, stored as
    mean/min/max/median fields. NOT counters: the rate is derived on a
    2s lattice before the first aggregation, so consumers must
    multiply by 8 for bits/s and must NOT apply derivative().
  retentionRules:
    - type: expire
      everySeconds: 628992000   # ~20y = 2x the 520w consuming task

---
apiVersion: influxdata.com/v2alpha1
kind: Task
metadata:
  name: downsample-52w-to-260w
spec:
  name: downsample-52w-to-260w
  description: Downsamples the 52w tier to 260w (mean/min/max/median cascade).
  every: 260w
  offset: 30s
  query: |

    src = from(bucket: "traffic-52w")
      |> range(start: -task.every)
      |> filter(fn: (r) => r._measurement == "network_interface")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_mean" or r._field == "statistics_in-octets_mean")
      |> aggregateWindow(every: 260w, fn: mean, createEmpty: false)
      |> to(bucket: "traffic-260w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_min" or r._field == "statistics_in-octets_min")
      |> aggregateWindow(every: 260w, fn: min, createEmpty: false)
      |> to(bucket: "traffic-260w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_max" or r._field == "statistics_in-octets_max")
      |> aggregateWindow(every: 260w, fn: max, createEmpty: false)
      |> to(bucket: "traffic-260w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_median" or r._field == "statistics_in-octets_median")
      |> aggregateWindow(every: 260w, fn: median, createEmpty: false)
      |> to(bucket: "traffic-260w")

---
apiVersion: influxdata.com/v2alpha1
kind: Bucket
metadata:
  name: traffic-520w
spec:
  name: traffic-520w
  description: >-
    Traffic RATES in octets/s at 520w resolution, stored as
    mean/min/max/median fields. NOT counters: the rate is derived on a
    2s lattice before the first aggregation, so consumers must
    multiply by 8 for bits/s and must NOT apply derivative().
  retentionRules:
    - type: expire
      everySeconds: 314496000   # ~10y terminal tier, no downstream task

---
apiVersion: influxdata.com/v2alpha1
kind: Task
metadata:
  name: downsample-260w-to-520w
spec:
  name: downsample-260w-to-520w
  description: Downsamples the 260w tier to 520w (mean/min/max/median cascade).
  every: 520w
  offset: 30s
  query: |

    src = from(bucket: "traffic-260w")
      |> range(start: -task.every)
      |> filter(fn: (r) => r._measurement == "network_interface")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_mean" or r._field == "statistics_in-octets_mean")
      |> aggregateWindow(every: 520w, fn: mean, createEmpty: false)
      |> to(bucket: "traffic-520w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_min" or r._field == "statistics_in-octets_min")
      |> aggregateWindow(every: 520w, fn: min, createEmpty: false)
      |> to(bucket: "traffic-520w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_max" or r._field == "statistics_in-octets_max")
      |> aggregateWindow(every: 520w, fn: max, createEmpty: false)
      |> to(bucket: "traffic-520w")

    src
      |> filter(fn: (r) => r._field == "statistics_out-octets_median" or r._field == "statistics_in-octets_median")
      |> aggregateWindow(every: 520w, fn: median, createEmpty: false)
      |> to(bucket: "traffic-520w")
